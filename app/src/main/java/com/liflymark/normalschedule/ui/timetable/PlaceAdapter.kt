package com.liflymark.normalschedule.ui.timetable

import androidx.fragment.app.Fragment
import com.liflymark.normalschedule.ui.Course

class PlaceAdapter(private val fragment: Fragment, private val placeList: List<Course>) {
}