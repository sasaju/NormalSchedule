<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NormalSchedule" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NormalSchedule.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.NormalSchedule.ShowTimeTableSplash" parent="Theme.NormalSchedule.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="Theme.NormalSchedule.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.NormalSchedule.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="IconTextViewStyle">
        <item name="android:textSize">24sp</item>
        <item name="android:gravity">center</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="AppWidgetDay" >
<!--        <item name="backgroundRadius">@dimen/widget_parent_radius_dimen</item>-->
        <item name="android:padding">@dimen/widget_parent_padding</item>
    </style>

    <style name="AppWidgetDayTextView1">
        <item name="android:textColor">#000000</item>
    </style>

    <style name="AppWidgetDayCourse">
        <item name="android:background">@color/appwidgetDayCourseBack</item>
    </style>

    <style name="AppWidgetDayCourseName">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="AppWidgetDayTeacher">
        <item name="android:textColor">@color/black</item>
        <item name="android:alpha">0.43</item>
    </style>

</resources>